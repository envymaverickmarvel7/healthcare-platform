services:
  backend:
    build: ../backend
    ports:
      - "5050:5050"
    env_file:
      - ../backend/.env
  booking-portal:
    build: ../apps/booking-portal
    ports:
      - "5174:80"
  patient-portal:
    build: ../apps/patient-portal
    ports:
      - "5177:80"
  doctor-admin:
    build: ../apps/doctor-admin
    ports:
      - "5175:80"
  pathology-panel:
    build: ../apps/pathology-panel
    ports:
      - "5176:80"
  analytics-dashboard:
    build: ../apps/analytics-dashboard
    ports:
      - "5173:80"
  mongo:
    image: mongo:8
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
